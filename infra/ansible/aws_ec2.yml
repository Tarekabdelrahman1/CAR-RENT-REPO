plugin: aws_ec2
regions:
  - us-east-1

filters:
  tag:Environment: dev
  instance-state-name: running

hostnames:
  - instance-id

keyed_groups:
  - key: tags.Tier
    prefix: tier

compose:
  ansible_aws_ssm_instance_id: instance_id
